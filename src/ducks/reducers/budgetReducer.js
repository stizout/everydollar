const InitialState = {
    budget: [],
}

const GET_BUDGET = 'GET_BUDGET';

export default function(state = InitialState, action) {
    // switch(action.type) {
    //     case GET_BUDGET:
    //         return {...state, budget}
    // }
    return state;
}